name: crimm
channels:
  - conda-forge
dependencies:
  - python>=3.10
  # Core dependencies (via conda)
  - numpy>=1.24
  - scipy>=1.11
  - biopython>=1.83
  - pandas>=2.0
  - requests>=2.31
  # Visualization
  - nglview>=3.1
  - ipywidgets>=8.1
  - jupyterlab>=4.2
  # Cheminformatics
  - rdkit>=2023
  - propka>=3.5.1
  # Install crimm from local source
  - pip
  - pip:
    - -e .
